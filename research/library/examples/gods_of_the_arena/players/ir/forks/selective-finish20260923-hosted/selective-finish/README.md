# Selective short structure finishes

Fresh400-game comparison: mean score1540.30→1512.32 (-1.82%). Deployment qualified: False. Read evidence/trial-report.json for intervals, frequencies, contexts and rival gaps.

Run `python verify.py`. Compile with `python convert.py compile --out /new/path`; extract with `python convert.py extract --source policy.bas --out /new/path`. The exact tested source must remain unchanged.
