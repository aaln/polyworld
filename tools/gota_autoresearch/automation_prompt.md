Scheduled GotA autoresearch wake-up (every 2 hours). Continue the GotA research loop in this session for https://github.com/aaln/polyworld (local clone /home/ubuntu/repos/polyworld, runtime /home/ubuntu/repos/polyworld/.gota).

Standing instruction from Aaron (2026-09-21, offline ~27h for Yom Kippur): the deployed Aaron/Coach policies are believed to be on a trajectory to #1. Priority is to MONITOR the live league (https://softmax.com/observatory/v2?tab=coworlds&detail=league:league_3c60897b-25cf-4b37-9d1a-8554c1198f28) and, only if there is evidence a rival is majorly beating our policies (repeated head-to-head losses of the current versions, or rank slipping over consecutive checks), fork the deployed IR and improve it toward #1. Trigger criteria and per-rival evidence live in `.gota/CHECKPOINT.md`. Ownership: take single-writer ownership (update `.gota/ownership.json`) only if the account shows no XP requests created by another writer since the last check; then hosted spend may resume within budget after the UTC day rolls over. Deploy improvements to Coach first and keep Aaron as rollback. Do not message anyone; the user is offline — report concisely in-session.

Do this cycle now:
1. Reconcile before acting: `git -C /home/ubuntu/repos/polyworld fetch origin` (do not discard local work), read `.gota/ownership.json`, `.gota/CHECKPOINT.md`, `.gota/xp-ledger.json` and any pending request receipts; then run `SOFTMAX_TOKEN="${SOFTMAX_USER_API_TOKEN}" .venv/bin/python tools/gota_autoresearch/remote.py monitor` from the fork root. Record the UTC check time, game version drift, live ranks and exact champion version UUIDs for Aaron, Coach, Richard, Alex Smith, Jordan and any other leader. If the check fails, leave the previous timestamp visibly stale and repair access.
2. Harvest/audit any pending hosted XP requests recorded in the journal before creating new ones. Never create a fresh idempotency key without reconciling the original request. Respect 400 episodes/cycle, 1,600/UTC day, <=3 active requests.
3. If the trigger criteria are NOT met, do a short local research step (rule-level probe / hypothesis test) and skip hosted spend. If they ARE met, follow docs/guides/devin-gota-autoresearch.md §5: choose one failure mechanism from recent losses/draws, build faithful episode IR, update the opponent model, have the coach propose one falsifiable repair (hypothesis ID, layer/rule/skill, trigger, predicted effect, falsifier, runtime cost), fork the seven-layer Python IR, compile/extract exactly, screen locally with responsive matches on both colors, then freeze and run a hosted comparison only through the durable idempotent budget journal.
4. Promote only an exactly qualified source within current authority (Aaron incumbent, Coach challenger, a-aron retired); verify membership readback and retain rollback. If nothing qualifies, retain the incumbent and record the null result.
5. Checkpoint: update `.gota/CHECKPOINT.md`, `.gota/progress.json`, `.gota/xp-ledger.json`, `.gota/ownership.json`; commit portable code/models/sanitized results to the aaln/polyworld fork branch (never push to Metta-AI/polyworld; never commit tokens or signed URLs).
6. Report to the user in one concise message: check time, live rank, exact versions/hashes, per-color W/L/D/invalid, distinct streams, mechanism evidence, gate results, spend/pending jobs, next discriminating test. No guaranteed-win claims. Do not message other people.

Finish the cycle within ~100 minutes so the next wake-up finds a consistent checkpoint.
