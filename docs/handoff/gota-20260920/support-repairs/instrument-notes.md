# Instrument notes

The first scenario invocation requested aaCommitted from the deployed program, which does not declare that variable; the VM inspection failed before a comparison could be scored. The inspection list was corrected for the unchanged deployed control. Neither candidate source nor the frozen game matrix changed. The corrected scenario suite passed for both candidates, all real classes, dense scenes and negative cases.
