Work in https://github.com/aaln/polyworld. Read AGENTS.md and docs/guides/devin-gota-autoresearch.md, then carry out its GotA research loop. Our objective is to beat the current Richard, Alex Smith and Jordan policies and attain and retain #1. Check the live league every three hours; keep improving when we fall behind and preserve evidence when a repair fails.

Start by reconciling the current champion versions, published engine, ownership, durable checkpoint and pending XP. Use tools/gota_autoresearch/remote.py for portable initialization, verification, build and read-only monitoring. Keep Aaron as the incumbent and Coach as the qualified challenger unless a newer explicit instruction says otherwise; a-aron stays retired. The repository archive is historical evidence, not a completed ownership transfer. Coordinate the existing local worker handoff before spending XP or changing memberships; do not run competing writers or reset spending on a new machine.

Use the same seven-layer Python IR as the primary policy. Diagnose losses through faithful episode IR with every matched rule, ordered multi-command behavior, observable beliefs and execution effects. Build/update opponent IR from predecision observable history with frozen holds, alternative explanations, coverage and trajectory novelty. Separate inferred models from source audits and validated reactive proxies. Use authentic Richard source for responding local matches where its identity is verified. Keep private opponent state out of our executable predicates.

Have the coach propose explicit hypotheses tied to rule/skill/layer, mechanism predictions, falsifiers and runtime cost. Fork, compile/extract exactly, test activation and full responsive games, then freeze and run fresh hosted comparisons through a durable idempotent budget journal. Record what actually changed and worked. Preserve all null results, regressions and invalid runs. Never tune on confirmation or silently relax gates. Deploy only the exact qualified source within current authority; verify membership and subsequent league performance and retain rollback.

At each checkpoint, commit portable code, models and sanitized results to this fork and retain raw evidence privately. Report live rank, exact versions/hashes, per-color W/L/D/invalid, distinct streams, mechanism evidence, gate results, spend/pending jobs and the next discriminating test. No guaranteed-win claims. If no candidate qualifies, retain the incumbent and continue research. Do not send messages to other people or publish credentials.
